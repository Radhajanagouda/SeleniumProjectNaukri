This project will have standard structure of initializing the drivers and POM model with waitFunctions added for framework.
This is basically BDD, Junit,Selenium Java project


---------------------------------------------------------------
Parallel execution
We implemented maven-sure-fire plugin parallel refer the pom.xml and its fired only 
from mvn clean test command

Parallel do not work when worked from feature file.